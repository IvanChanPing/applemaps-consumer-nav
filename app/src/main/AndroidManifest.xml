<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Consumer maps.apple.com renderer + existing network place/route providers. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- "Show your current location" control -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Declared for Ferrostar's merged optional foreground-service component. This app passes a null
         ForegroundServiceManager, so navigation does not request or post a notification at runtime. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppleMaps"
        tools:ignore="NotificationPermission">

        <!-- Google Maps SDK key (Road Level Details on-road lanes). Injected from -PMAPS_API_KEY (uncommitted).
             Allowlist the key in Google Cloud for com.example.applemaps.consumernav + debug SHA-1
             B3:B5:C0:BD:F6:72:AF:3A:B2:EF:DB:FB:40:30:7F:9F:18:B6:22:13, or use an unrestricted key, else the map is blank. -->
        <meta-data android:name="com.google.android.geo.API_KEY" android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:theme="@style/Theme.AppleMaps">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
